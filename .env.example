# Recall.ai (shared server-side credential)
RECALL_API_KEY=your-recall-api-key
RECALL_REGION=us-west-2

# Public URL — how Recall's bot reaches this server
# For local dev: use ngrok (ngrok http 3000)
# For Railway: leave unset, auto-detected from RAILWAY_PUBLIC_DOMAIN
PUBLIC_URL=https://your-subdomain.ngrok-free.app

PORT=3000

# Runway tools A/B:
# - backend_rpc: send the same minimal backend_rpc tool used by Contact Meeting tests
# - client_event: send a minimal client_event tool for console logging tests
# - empty: send tools: []
# - off: omit the tools key and keep the original POC-clean payload
RUNWAY_TOOLS_MODE=backend_rpc

# Runway API keys are entered per-user in the browser UI (not needed here)
